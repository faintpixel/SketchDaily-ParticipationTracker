<?xml version="1.0"?>
<configuration>
  <appSettings>
    <!-- need to set these to run -->
    <add key="Username" value="artomizer" />
    <add key="Password" value="" />
    <add key="CacheDirectory" value="/home/pi/SketchDaily/ParticipationTracker/Cache/" />

    <!-- should set these -->
    <add key="UserAgent" value="flair bot for /r/sketchdaily by /u/artomizer"/>

    <!-- can leave these -->
    <add key="ParticipantsFile" value="/home/pi/SketchDaily/ParticipationTracker/Files/Participants.txt" />
    <add key="BlacklistFile" value="/home/pi/SketchDaily/ParticipationTracker/Files/BlackList.txt" />
    <add key="APIDelay" value="2500" />
    <!-- Must be at least 2000 (2 seconds) as per the reddit API rules -->
  </appSettings>

  <startup></startup>

</configuration>
